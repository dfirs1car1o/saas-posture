version: 1
roles:
  malware_analyst:
    allowed_tools:
      - sift.memory.analyze
      - sift.disk.timeline
      - sift.pcap.extract_ioc
      - storage.read.evidence
      - storage.write.artifacts
      - case.update
  triage_analyst:
    allowed_tools:
      - storage.read.evidence
      - sift.pcap.extract_ioc
      - case.update
  report_writer:
    allowed_tools:
      - case.read
      - storage.read.artifacts
      - report.generate
  release_gate:
    allowed_tools:
      - release.validate
      - release.approve
      - release.rollback
